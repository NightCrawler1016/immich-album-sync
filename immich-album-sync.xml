<?xml version="1.0"?>
<Container version="2">

  <!-- ═══════════════════════════════════════════════════════════════════
       Immich Album Sync — Unraid Community Applications Template
       Maintained at: https://github.com/NightCrawler1016/immich-album-sync
       ═══════════════════════════════════════════════════════════════════ -->

  <Name>immich-album-sync</Name>
  <Repository>ghcr.io/nightcrawler1016/immich-album-sync:latest</Repository>
  <Registry>https://github.com/NightCrawler1016/immich-album-sync/pkgs/container/immich-album-sync</Registry>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>

  <!-- Links -->
  <Support>https://github.com/NightCrawler1016/immich-album-sync/issues</Support>
  <Project>https://github.com/NightCrawler1016/immich-album-sync</Project>
  <Overview>
    One-way album sync between two Immich servers with a responsive web UI.

    Designed for Unraid users who run a private Immich server (full library) and a public/family-facing Immich server (curated albums only). Configure sync jobs, schedules, and API keys entirely through the browser — no config files or environment variables needed after the initial setup.

    Features:
    - Web UI with password protection
    - Multiple sync jobs with independent schedules
    - Preserves original files, EXIF, GPS, and Apple Live Photos (.HEIC + .MOV)
    - Duplicate-safe (uses immich-go for smart detection)
    - Real-time log streaming in the browser
    - Mobile-responsive design
    - Add-only mode by default (safe for family sharing)
  </Overview>
  <Category>Backup: MediaApp:Photos Status:Stable</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <!-- TemplateURL intentionally omitted — prevents Unraid from re-fetching
       defaults from GitHub and overwriting user-configured settings.
       To update the template manually, re-download from:
       https://raw.githubusercontent.com/NightCrawler1016/immich-album-sync/main/immich-album-sync.xml -->
  <TemplateURL></TemplateURL>
  <Icon>https://raw.githubusercontent.com/NightCrawler1016/immich-album-sync/main/icon.png</Icon>
  <ExtraParams>--restart unless-stopped</ExtraParams>
  <Description>
    One-way album sync between two Immich servers with a responsive web UI. Built for users who run a private Immich (full library) and a public or family-facing Immich (curated albums only). Configure sync jobs, schedules, and API keys through the browser. Supports Apple Live Photos, duplicate detection via immich-go, and real-time log streaming. Default login: admin / admin — change on first use.
  </Description>

  <DonateText>Support the project on GitHub</DonateText>
  <DonateLink>https://github.com/NightCrawler1016/immich-album-sync</DonateLink>

  <!-- ═══════════════════════
       PORTS
       ═══════════════════════ -->

  <Config
    Name="Web UI Port"
    Target="8080"
    Default="8080"
    Mode="tcp"
    Description="Port for the Immich Album Sync web interface. Open this in your browser after the container starts."
    Type="Port"
    Display="always"
    Required="true"
    Mask="false">8080</Config>

  <!-- ═══════════════════════
       PATHS
       ═══════════════════════ -->

  <Config
    Name="App Data"
    Target="/app/appdata"
    Default="/mnt/user/appdata/immich-album-sync"
    Mode="rw"
    Description="Persistent storage for the database (config.db), download cache, and sync logs. Map to a location on your array."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/immich-album-sync</Config>

  <!-- ═══════════════════════
       ENVIRONMENT VARIABLES
       ═══════════════════════ -->

  <Config
    Name="Secret Key"
    Target="SECRET_KEY"
    Default="change-me-please-use-a-long-random-string-here"
    Mode=""
    Description="REQUIRED: Change to a unique random string used to sign session cookies and encrypt stored API keys. Using the default value is INSECURE. Key length: minimum 16 characters | recommended 32–64 characters | maximum 128 characters (longer values provide no extra security). Generate a strong key at: https://1password.com/password-generator/ — select 32–64 characters, all character types. WARNING: changing this key after initial setup will invalidate all stored API keys and log out all active sessions — you will need to re-enter API keys for every sync job."
    Type="Variable"
    Display="always"
    Required="true"
    Mask="true">change-me-please-use-a-long-random-string-here</Config>

  <Config
    Name="Timezone"
    Target="TZ"
    Default="America/New_York"
    Mode=""
    Description="Container timezone. Used for log timestamps. Examples: America/New_York, America/Chicago, America/Los_Angeles, Europe/London, UTC. Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">America/New_York</Config>

  <Config
    Name="PUID"
    Target="PUID"
    Default="99"
    Mode=""
    Description="User ID the app process runs as (the container no longer runs as root). The default 99 is Unraid's 'nobody' user, which matches standard appdata ownership — leave it unless your appdata/cache is owned by a different user. If the container logs permission errors writing to appdata, set this to the owner UID of your appdata share."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">99</Config>

  <Config
    Name="PGID"
    Target="PGID"
    Default="100"
    Mode=""
    Description="Group ID the app process runs as. The default 100 is Unraid's 'users' group, which matches standard appdata ownership. Change only if your appdata/cache is owned by a different group."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">100</Config>

  <Config
    Name="Cleanup Cache After Upload"
    Target="CLEANUP_CACHE"
    Default="false"
    Mode=""
    Description="Set to 'true' to automatically delete downloaded originals from the cache directory after they have been successfully uploaded. Saves disk space but files will be re-downloaded on the next sync run. Can also be toggled per-job in the Web UI."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">false</Config>

  <Config
    Name="Cache Directory Path"
    Target="CACHE_PATH"
    Default="/app/appdata/cache"
    Mode=""
    Description="Path inside the container where original files are temporarily staged before uploading to the destination server. The default stores the cache inside the App Data directory.

To store the cache on a different disk, SSD pool, or network share (recommended for large first-time syncs):
  1. In the Docker template, add a second Path mapping:
       Container path: /app/cache
       Host path: /mnt/user/YourDisk/immich-sync-cache  (or any writable path)
  2. Set this field to: /app/cache

This lets you route multi-GB or multi-TB downloads to a large array disk or NAS share without filling your primary Unraid cache pool. The container must have write access to whatever path you choose."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">/app/appdata/cache</Config>

  <Config
    Name="Max Cache Batch Size (MB)"
    Target="BATCH_SIZE_MB"
    Default="10240"
    Mode=""
    Description="Maximum amount of data (in megabytes) to download before uploading a batch to the destination server. After each batch is uploaded it is cleared from the cache, then the next batch begins. This prevents the local cache from growing too large during first-time syncs of very large albums. Default: 10240 (10 GB). Set to 0 to download all files before uploading (disables size-based batching)."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">10240</Config>

  <Config
    Name="Max Files Per Batch"
    Target="BATCH_FILE_COUNT"
    Default="0"
    Mode=""
    Description="Maximum number of files to stage per batch before uploading. Works alongside Max Cache Batch Size — whichever limit is reached first triggers an upload cycle. Default: 0 (no file-count limit; only the size limit applies). Example: set to 500 to process albums in groups of 500 files regardless of total size."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">0</Config>

  <Config
    Name="Log File Path"
    Target="LOG_PATH"
    Default="/app/appdata/logs/sync.log"
    Mode=""
    Description="Internal path for the sync log file. Viewable live in the Web UI under 'Live Logs'. Leave blank to use the default."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/app/appdata/logs/sync.log</Config>

  <Config
    Name="Database Path"
    Target="DB_PATH"
    Default="/app/appdata/config.db"
    Mode=""
    Description="Internal path for the SQLite database that stores sync job configurations and run history. Leave blank to use the default."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">/app/appdata/config.db</Config>

</Container>
